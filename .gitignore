# Реальные реквизиты ИП и готовые к публикации юридические страницы.
# Репозиторий публичный — эти файлы содержат реальные ФИО/адрес/телефон/email/банковские реквизиты
# и не должны попадать в git ни разу (даже в старый коммит — историю потом не вычистить просто так).
# На проде подставляются автоматически при сборке на Vercel (см. build-legal.js) из Environment
# Variables проекта; private/ — только для локального просмотра с реальными данными.
private/

# Служебный конфиг Claude Code (локальный запуск браузерного превью) — не часть сайта.
.claude/
